;; profile for org mode

;; turncate the line automatically
;;(add-hook 'org-mode-hook (lambda () (setq toggle-truncate-lines t)))
(add-hook 'org-mode-hook (lambda () (setq truncate-lines nil)))


(provide 'init-org)
